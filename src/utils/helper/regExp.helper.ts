export const regExpImage = new RegExp(
  /[^\s]+(.*?).(jpg|jpeg|png|gif|JPG|JPEG|PNG|GIF|jfif)$/
);
export const regExpDocx = new RegExp(/[^\s]+(.*?).(docx|doc|DOC|DOCX)$/);
export const regExpExcel = new RegExp(
  /[^\s]+(.*?).(csv|xlsx|xls|CSV|XLSX|XLS)$/
);
export const regExpPdf = new RegExp(
  /[^\s]+(.*?).(pdf|PDF)$/
);
export const regExpPowerpoint = new RegExp(
  /[^\s]+(.*?).(ppt|pptx|PPT|PPTX)$/
);